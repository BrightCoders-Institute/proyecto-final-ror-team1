<h1>Shipments</h1>

<%= form_tag shipments_index_path, method: :get do %>
  <div>
    <%= label_tag :status, "Status:" %>
    <%= select_tag :status, options_for_select(["", "IN TRANSIT", "DELIVERED"], params[:status]) %>
  </div>

  <div>
    <%= label_tag :carrier_name, "Carrier:" %>
    <%= select_tag :carrier_name, options_for_select(["", "FEDEX", "DHL"], params[:carrier_name]) %>
  </div>

  <div>
    <%= submit_tag "Filter" %>
  </div>
<% end %>

<table>
  <thead>
    <tr>
      <th>Status</th>
      <th>Carrier</th>
    </tr>
  </thead>
  <tbody>
    <% @shipments.each do |shipment| %>
      <tr>
        <td><%= shipment.status %></td>
        <td><%= shipment.carrier.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>
